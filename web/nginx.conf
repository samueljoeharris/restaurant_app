server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Same-origin proxy for Firebase Auth redirect flow (required on custom domains;
    # see https://firebase.google.com/docs/auth/web/redirect-best-practices).
    # ^~ beats the static *.js regex below — otherwise handler.js / iframe.js 404.
    location ^~ /__/auth {
        proxy_pass https://ttf-restaurant-dev.firebaseapp.com;
        proxy_ssl_server_name on;
        proxy_set_header Host ttf-restaurant-dev.firebaseapp.com;
    }

    location ^~ /__/firebase {
        proxy_pass https://ttf-restaurant-dev.firebaseapp.com;
        proxy_ssl_server_name on;
        proxy_set_header Host ttf-restaurant-dev.firebaseapp.com;
    }

    location / {
        # Required for signInWithPopup when VITE_GOOGLE_AUTH_USE_POPUP=true.
        add_header Cross-Origin-Opener-Policy "same-origin-allow-popups" always;
        # CSP is set on the HTML document only; the browser applies the
        # document's policy to every subresource it loads, so the static-asset
        # location below intentionally does NOT repeat this header.
        add_header Content-Security-Policy "default-src 'self'; script-src 'self' https://maps.googleapis.com https://www.gstatic.com https://www.google.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob: https://maps.googleapis.com https://maps.gstatic.com https://*.googleusercontent.com https://api.qrserver.com; connect-src 'self' https://api.dev.littlescout.app https://api.littlescout.app https://identitytoolkit.googleapis.com https://securetoken.googleapis.com https://firebaseappcheck.googleapis.com https://firebasevertexai.googleapis.com https://maps.googleapis.com; frame-src 'self' https://www.google.com https://*.firebaseapp.com; base-uri 'self'; form-action 'self'; object-src 'none'; frame-ancestors 'none'" always;
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
