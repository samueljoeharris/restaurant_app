# Local secrets workflow — see .env.defaults for non-secret config.
#
#   gcloud auth application-default login    # once on Mac
#   ./scripts/sync-secrets.sh              # pulls from GCP Secret Manager → .secrets/
#
# Cursor Cloud: one Runtime Secret GCP_DEV_SYNC_SA_JSON (dev-sync SA JSON).
# See docs/SECRETS_MATRIX.md and docs/CLOUD_AGENT.md
