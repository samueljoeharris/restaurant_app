# DEPRECATED — use the split files instead:
#
#   .env.cloud.visible.example     → Cursor Environment variables (agent can see)
#   docs/CLOUD_AGENT.md            → Runtime Secrets list (agent cannot see values)
#
# Real Firebase is the default (not emulator). See docs/CLOUD_AGENT.md.
