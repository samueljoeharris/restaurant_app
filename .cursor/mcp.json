{
  "mcpServers": {
    "github": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--env-file",
        "${workspaceFolder}/.env",
        "ghcr.io/github/github-mcp-server"
      ]
    },
    "gcloud": {
      "command": "${workspaceFolder}/.cursor/scripts/gcloud-mcp.sh",
      "args": []
    }
  }
}
