{
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": ["-y", "firebase-tools@latest", "mcp"]
    },
    "github": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--env-file",
        "${workspaceFolder}/.secrets/mcp.env",
        "ghcr.io/github/github-mcp-server"
      ]
    },
    "gcloud": {
      "command": "${workspaceFolder}/.cursor/scripts/gcloud-mcp.sh",
      "args": []
    }
  }
}
