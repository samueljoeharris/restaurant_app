Settings / alert switch. Sky-blue track when on.

```jsx
<Toggle checked={digest} onChange={setDigest} label="Weekly digest" />
```

Use for notification & alert preferences (e.g. "Kid food gets faster"). Omit `label` to place the bare switch in a row that supplies its own text.
